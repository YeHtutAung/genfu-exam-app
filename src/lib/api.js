// Thin fetch wrappers for Vercel Edge Functions:
//   explain(questionId, questionJp, hintJp)  → POST /api/explain
//   uploadBundle(formData)                   → POST /api/upload-bundle
//   confirmUpload(bundleToken)               → POST /api/confirm-upload
